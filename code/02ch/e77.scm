;; First because originally the functions were essentially private since
;; they were defined inside of the package. all that the code is doing is exporting
;; the functions for use outside of the package internals
