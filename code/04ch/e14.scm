;; I do think that it is because the built in definition of map expects data
;; that is in very different form than what is actually provided by the interpreter
;; while the standard definition of map, by Eva, is made to work with the data
;; representaion of code natively.
